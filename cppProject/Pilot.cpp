#include "Pilot.h"

Pilot::Pilot(const char* name, bool isCaptian, Address* address) : adderss(adderss), CrewMember(name) {
	this->isCaptian = isCaptian;
	
}
Pilot::Pilot(const char* name, bool isCaptian) : adderss(adderss), CrewMember(name) {
	this->isCaptian = isCaptian;

}
